# Rules

## Git
- NEVER run `git restore` — it destroys uncommitted work with no recovery path.
- NEVER run `rm -rf` on untracked directories without confirming with the user first.
- Before any destructive operation, stash or back up changes explicitly.
